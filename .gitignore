# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# env files (IMPORTANT)
.env
.env.*
!.env.example

# build output (already partially there but keep explicit)
dist/
dist-ssr/

# coverage (if tests added later)
coverage/

# local Vercel (optional but good)
.vercel/

# logs (you already have but safe to keep)
logs/

# dependency cache (optional cleanup)
.pnpm-store/